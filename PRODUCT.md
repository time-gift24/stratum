# 产品说明：运筹 Stratum

## 产品类型

`product`

## 平台

`web`

## 产品定位

Stratum 是一个 Rust 优先的 Agent Runtime 和工作流编排系统。运筹 Stratum 是它面向人的 Agent OS，用户在一个连续的产品环境中选择 Agent、发起对话、查看真实执行状态，并在未来通过可视化画布配置工作流。

它不是换皮聊天机器人，不是云控制台，也不是以视觉特效为主的 AI 展示站。产品通过可靠执行、渐进式透明和清楚的操作层级建立信任。

## 用户

主要用户通过对话处理真实任务。他们希望 Agent 能够持续推进工作，不需要理解 Runtime、事件协议、工具注册或其他开发者术语。

次要用户负责配置 Agent 模板、编排工作流和部署 Agent。他们需要执行可见性、审批与恢复能力，但这些能力不应侵入最终用户的对话界面。

## 产品结构

### 共享产品壳层

所有页面共享品牌、一级导航、运行状态、语言与最近对话。桌面端使用悬浮顶部栏和窄工具轨，移动端使用同一套导航数据构建可访问抽屉。壳层不能占用本应属于工作区的横向空间。

### 概览 `/`

概览把真实输入、Agent、模型和最近对话放进一张可选择的运行关系图，并在右侧显示当前节点的真实属性与下一步操作。任何数量、状态和列表都来自 API 或本地持久化事实。没有数据时诚实留空，不生成示例内容。

### 对话 `/chat`

对话页面保留可读的单列消息、固定 Composer、历史恢复与渐进式透明。Composer 使用大输入面、底部配置工具和独立发送控制，Agent、模型与思考状态始终围绕真实会话配置。思考、工具执行和中间步骤默认折叠，用户需要时可以展开真实详情。

### 未来画布

画布面向工作流配置和编排。它使用导航、工作区和参数区的三分结构，以节点和类型化连接表达 Agent、工具、输入与输出关系。画布与对话共享产品视觉语言，但不会把开发者控制项放入普通对话流程。

## 品牌人格

克制、可信、精密、安静。界面体现严肃基础设施的稳定感，不喧闹、不玩具化，也不依靠拟人角色吸引注意力。

“运筹”是现有品牌名称。中国文化语境只通过构图秩序、留白、墨色层级、玉青焦点、少量暖色状态和收放有度的交互节奏体现。界面不使用文言包装、主题化功能名称、书法字体、仿古纹样或解释这种风格的小字。

## 设计原则

1. **Agent OS 优先。** 页面名称、操作和状态使用直接的产品语言。
2. **渐进式透明。** 思考、工具执行和中间步骤默认折叠，但始终可以按需查看。
3. **单一视觉重心。** 每个页面只有一个主要任务，导航、状态和配置主动退后。
4. **视觉代替装饰性说明。** 使用尺寸、位置、对比、留白和状态变化引导，不添加无功能的小字、眉题或伪技术参数。
5. **工程严谨服务最终用户。** 运行可靠性和可观察性存在于真实状态中，不把 Runtime 术语变成用户负担。
6. **任务界面各守边界。** 对话面向最终用户，画布面向配置和编排。
7. **动效只表达反馈。** 点击、选择、开合、连接和运行状态可以使用短促反馈，不做环境式循环动画。
8. **真实数据优先。** 不伪造用量、额度、模型、Agent、会话、审批或执行结果。

## 反面参考

- ChatGPT 克隆、客服聊天窗口和换皮 AI 助手。
- 用“战略、局势、筹策、推演”等主题化文案解释品牌气质。
- 无功能的小字提示、装饰编号、伪终端参数和持续滚动状态文案。
- 大面积霓虹、高饱和渐变、玻璃卡片、全局颗粒，以及工作区之外没有操作语义的装饰网格。
- 卡片套卡片、三张等宽功能卡片、虚构指标和营销 Hero。
- 仿古纹样、书法字体、印章动画、祥云和具象卷轴效果。

## 可访问性

正文对比度至少为 4.5:1，大号文本至少为 3:1。所有主要流程支持键盘导航、屏幕阅读器名称和至少 44px 的触控目标。移动抽屉正确管理焦点与 Escape 关闭。所有动效提供 `prefers-reduced-motion` 结果，移动端不得把桌面画布整体缩小，必须改为真实单列信息流。
