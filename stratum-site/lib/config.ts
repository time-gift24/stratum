/** 站点级常量。链接与地址集中在此，替换只改这一处。 */
export const siteConfig = {
  // TODO(replace-before-launch): 替换为真实 GitHub 仓库地址
  githubUrl: "https://github.com/stratum-replace-me",
  /** 对话页路径；落地页与 app 同域部署时生效 */
  conversationPath: "/conversation",
  name: { zh: "运筹 Stratum", en: "Stratum" },
} as const;
